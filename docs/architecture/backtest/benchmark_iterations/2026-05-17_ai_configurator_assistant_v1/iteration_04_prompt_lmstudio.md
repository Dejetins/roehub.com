# Iteration 04 Prompt contract + LM Studio adapter

Дата: 2026-05-18.

Статус: accepted, direct-main delivery completed.

## Цель

Добавить canonical English system prompt, JSON output contract, LM Studio
`POST /v1/chat/completions` adapter, and smoke coverage for structured responses.

## Предварительный gate

Iteration 03 проверен перед началом:

- `implementation_progress.json`: `03-conversation-api-storage.accepted=true`;
- `next_iteration_allowed=true`;
- `iteration_03_conversation_api.json`: `pushed_to_main=true`;
- `macstudio_verified=true`;
- recorded accepted commit: `32dc379c36f6e376f7ab17fd5167c5bce6fb68e8`.

## Что изменено

- Добавлен canonical English prompt `SYSTEM_PROMPT_ID: backtest_ai_configurator_assistant_v1`.
- Добавлен prompt package builder с секциями:
  - `TRUSTED_CONTEXT_JSON`;
  - `CURRENT_FORM_CONFIG_JSON`;
  - `RECENT_CHAT_CONTEXT_JSON`;
  - `USER_MESSAGE`;
  - `OUTPUT_JSON_SCHEMA`;
  - `OUTPUT_JSON_EXAMPLE`.
- Добавлен backend output schema с обязательным `conversation_title`.
- Добавлен `LMStudioOpenAICompatibleAdapter`.
- Runtime config переведен на `runtime: lm_studio`.
- Runtime readiness smoke теперь проверяет structured `POST /v1/chat/completions`;
  `/v1/models` не считается достаточной готовностью.
- Добавлен `scripts/backtest_ai/run_lmstudio_prompt_contract_smoke.py`.
- `Deploy Backend` теперь включает `scripts/backtest_ai` in `/opt/roehub/app`.
- Обновлены deterministic gateway output и validator под новый envelope.
- Создан contract doc: `prompt_contract.md`.

## Prompt contract

Prompt/schema paths:

```text
src/trading/contexts/backtest/application/ai_configurator/prompts/assistant_v1.py
src/trading/contexts/backtest/application/ai_configurator/schema.py
```

Hard scope in system prompt:

- no backtest execution;
- no secrets/files/tools/APIs/terminals;
- no model tool/function calling;
- one-symbol config only;
- user input is untrusted.

Output envelope:

```text
schema_version
intent
status
assistant_message
conversation_title
config
unsupported_items
clarifying_questions
warnings
```

`conversation_title` is generated by the model, backend-validated, and stored under
Iteration 03 conversation title semantics.

## LM Studio

Adapter path:

```text
src/trading/contexts/backtest/adapters/outbound/ai_config_agent/lmstudio_chat_completions.py
```

Payload notes:

- endpoint: `POST /v1/chat/completions`;
- `response_format.type=json_schema`;
- `response_format.json_schema.strict=true`;
- `tools` not sent;
- `tool_choice` not sent;
- no nullable-union `type` arrays in the LM Studio schema;
- `config` can be an object or `null` through compact `oneOf`, matching backend
  status rules.

The response schema sent to LM Studio is compact and strict at the envelope level.
The full backend schema remains the validation authority after the model response.

## Контрактное влияние

| Surface | Classification | Notes |
| --- | --- | --- |
| Public API contract | none | Browser-visible conversation endpoints from Iteration 03 are not changed. |
| Model output schema | breaking-change | Assistant envelope changes from old draft shape to strict Iteration 04 schema. |
| Prompt contract | breaking-change | Canonical prompt/package becomes the current assistant contract. |
| Config schema | compatible-change | Runtime literal changes from pending placeholder to `lm_studio`; existing config keys remain. |
| Persisted schema | none | No migration or table shape change. |
| Runtime workflow | compatible-change | Backend can call LM Studio, but no backtest execution/load gate is enabled. |
| Tool trust boundary | compatible-change | Explicitly confirms no model tools/function calling. |

## Проверки

Completed locally:

```text
uv run pytest -q tests/unit/contexts/backtest/application/ai_configurator
```

Result: `50 passed`.

```text
uv run ruff check src/trading/contexts/backtest/application/ai_configurator apps/worker/backtest_ai_configurator tests/unit/contexts/backtest/application/ai_configurator src/trading/contexts/backtest/adapters/outbound/ai_config_agent src/trading/contexts/backtest/adapters/outbound/config/backtest_ai_configurator_runtime_config.py scripts/backtest_ai/run_lmstudio_prompt_contract_smoke.py scripts/macos/lmstudio_backtest_ai_runtime.py
```

Result: passed.

```text
uv run pyright
```

Result: passed, `0 errors`.

```text
uv run python -m tools.docs.generate_docs_index --check
```

Result: passed.

## Mac Studio

Pre-delivery smoke ran in isolated temp worktree `/tmp/roehub-iter04-smoke` with the
current local diff applied on top of `00cd1640e60852fbb12c78efd150dafc84668360`.

Runtime ensure command:

```text
PYTHONPATH=src:. /opt/roehub/app/.venv/bin/python \
  scripts/macos/lmstudio_backtest_ai_runtime.py ensure \
  --config configs/prod/backtest_ai_configurator.yaml \
  --artifact /tmp/roehub-iter04-lmstudio-runtime.json \
  --json
```

Result after the compact `config` object/null schema alignment:

- `accepted=true`;
- endpoint `POST /v1/chat/completions`;
- `response_format=json_schema`;
- `assistant_v1_runtime_contract=chat_completions_ready`;
- loaded instance `gemma-4-e2b-it-4bit`.

Prompt contract smoke command:

```text
PYTHONPATH=src:. /opt/roehub/app/.venv/bin/python \
  scripts/backtest_ai/run_lmstudio_prompt_contract_smoke.py \
  --config configs/prod/backtest_ai_configurator.yaml \
  --artifact /tmp/roehub-iter04-prompt-smoke-10.json \
  --direct-count 10 \
  --generate-count 10 \
  --repair-count 10 \
  --json
```

Result:

| Smoke | Passed | Failed |
| --- | ---: | ---: |
| Direct structured LM Studio | 10/10 | 0 |
| Adapter generate | 10/10 | 0 |
| Adapter repair prompt | 10/10 | 0 |

Raw model outputs were redacted; artifact stores content hashes and failure summaries.

## Delivery

Direct-main delivery completed.

Implementation commit:

```text
585cddc6d89e2ca3a89c9b9e76baf372141eb151
```

Delivery packaging fix commit:

```text
30b5153754ee4d8de224a80dd947a009642d225f
```

Root-cause during delivery:

- deployed `/opt/roehub/app` initially had `scripts/backtest` and `scripts/macos`
  but not `scripts/backtest_ai`;
- `Deploy Backend` was updated to copy `scripts/backtest_ai` through path trigger,
  layout validation, rsync, and tar fallback.

GitHub runs:

| Commit | Workflow | Run | Result |
| --- | --- | ---: | --- |
| `585cddc6` | CI | 26017219058 | success |
| `585cddc6` | Deploy Backend | 26017219065 | success |
| `585cddc6` | Publish App Image | 26017219067 | success |
| `585cddc6` | Deploy Web | 26017261917 | success |
| `30b51537` | CI | 26017505185 | success |
| `30b51537` | Deploy Backend | 26017505197 | success |

Final Mac Studio source checkout:

```text
30b5153754ee4d8de224a80dd947a009642d225f
```

Deployed runtime verification from `/opt/roehub/app`:

| Smoke | Passed | Failed |
| --- | ---: | ---: |
| Direct structured LM Studio | 10/10 | 0 |
| Adapter generate | 10/10 | 0 |
| Adapter repair prompt | 10/10 | 0 |

Deployed smoke artifact:

```text
/opt/roehub/state/backtest_ai_configurator/iteration_04_prompt_lmstudio_smoke.json
```

Runtime file hashes in `/opt/roehub/app`:

```text
f9b54f10780376ea77743170ea7fe3e48720b74f1ee5af349924f35e3ff00673  scripts/backtest_ai/run_lmstudio_prompt_contract_smoke.py
94e64aca45d443e664feafdefa9133e0c22733eaf5ad6686e6b0a81ab6e4d627  src/trading/contexts/backtest/application/ai_configurator/schema.py
95abf03e1aea96d8ce816a9b5cd9ceb124f7a0858fd09fb8f4b6178d27dae67b  src/trading/contexts/backtest/application/ai_configurator/prompts/assistant_v1.py
1ab27d91df9024f9fc455ff98dbf644d50d9544fdfd8bdef323f81ba162dbc96  src/trading/contexts/backtest/adapters/outbound/ai_config_agent/lmstudio_chat_completions.py
0c9179c3d6391da826f1c2d387c645a50fbc9428b2a2fd41fa24b65b7497009a  configs/prod/backtest_ai_configurator.yaml
```

Final marker:

- `accepted=true`;
- `next_iteration_allowed=true`;
- `pushed_to_main=true`;
- `macstudio_verified=true`.
