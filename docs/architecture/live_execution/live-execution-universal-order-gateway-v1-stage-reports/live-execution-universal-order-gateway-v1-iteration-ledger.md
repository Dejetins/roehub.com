# Live Trading Universal Order Gateway v1 — журнал итераций

Журнал фиксирует выполнение stages для документа `docs/architecture/live_execution/live-execution-universal-order-gateway-v1.md`.

Статус: Stage `17` blocked; production-readiness monitoring/runbook assets
and pending-message self-heal are delivered on `main`, but the required fresh
full E2E fill/latency/slippage/browser proof is not complete.

## Правила Журнала

| Правило | Требование |
|---|---|
| Обновление после каждого stage | Исполнитель обязан обновить этот журнал после валидации stage: статус, evidence, blockers, contract impact, rollback, handoff. |
| Tests не являются acceptance | Unit/integration tests, ruff, pyright и docs check обязательны как quality gates, но stage считается accepted только после реальных вызовов к нужным runtime-boundaries. |
| No secret leakage | Нельзя писать API keys, secrets, cookies, tokens, raw Authorization headers, raw exchange responses с чувствительными полями. |
| Dependent stages blocked | Если stage зависит от недоказанного сервиса или контракта, следующий stage не может быть принят как production-ready. |
| Direct-main delivery | После successful validation stage может быть доставлен direct-main только если здесь записаны evidence, rollback path и deploy/CI status. |

## Обязательные Поля Stage Report

| Поле | Что фиксировать |
|---|---|
| Files changed | Все созданные/измененные code/config/docs/infra/test файлы. |
| Documentation sync | Какие связанные docs обновлены, какие оставлены без изменения и почему. |
| Service calls | Какие реальные API/internal/DB/Redis/exchange вызовы выполнены. |
| Error behavior | Какие failure cases проверены фактическими вызовами, не только tests. |
| Runtime config | Какие env/YAML/feature flags/kill switches добавлены или изменены, и какой fail-closed default доказан. |
| Latency/slippage | Какие hot-path latency/slippage metrics собраны или почему stage еще не касается hot path. |
| Logging/redaction | Доказательство, что logs/reports/artifacts не содержат secrets и signed payloads. |
| Monitoring | Prometheus/Monit/health evidence, alert/runbook impact. |
| Contract impact | Public/API, persistence, Redis, config, runtime, UI/browser. |
| Rollback | Как безопасно отключить stage без потери ledger/audit данных. |

## Stage Status

| Stage | Назначение | Статус | Блокеры | Следующий handoff |
|---|---|---|---|---|
| `01` | Current-state baseline: Backtest results, Strategy CRUD/run/stop, Live Runner, Redis streams, exchange connections, bindings, runtime ops. | accepted | Initial local probe was not the target runtime. Follow-up SSH evidence on `macstudio` confirmed API, Postgres, Redis, exchange-control, Monit and most Prometheus targets are available. Authenticated browser inventory for `/backtests`, `/strategies`, and `/settings` completed. `backtest-artifact-publisher` remains `up=0` / port `9203` closed, but the Mac Studio runbook excludes it from automatic startup, so it is current ops state rather than a Stage 01 blocker. | Direct-main publish/deploy handoff completed before Stage `02`. |
| `02` | Backtest variant -> immutable strategy creation. | accepted | None. Runtime API/SQL/browser evidence recorded after direct-main deploy. | Передать owner-scoped launchable variant contract, immutable strategy/provenance, idempotency, bounded metrics and rejected cases to Stage `03`. |
| `03` | `LiveStrategyProfile`: mode, exchange binding, sizing, limits, readiness. | accepted | None. Runtime API/SQL/metrics/browser evidence recorded after direct-main deploy. | Передать profile schema/API, safe defaults, readiness reasons, no-live-without-recent-auth evidence, and live-ready-only-with-recent-auth-plus-eligible-connection proof. |
| `04` | Strategy run/stop/restart lifecycle hardening. | accepted | None. Runtime API/SQL/worker/browser evidence recorded after direct-main deploy; production-found Postgres metadata serialization bug repaired in follow-up commit before acceptance. | Передать accepted run/restart command semantics, UI controls, one-active-run proof, worker pickup/drain evidence to Stage `05`. |
| `05` | Live signal evaluator and `StrategySignal` journal. | accepted | None. Runtime Redis/DB/API/browser/metrics evidence recorded after direct-main deploy; first smoke attempt used an invalid non-canonical candle `source` and was correctly dropped/acked before rerun with `source=ws`. | Передать accepted evaluator contract, `/strategies` signal journal, monitor/paper mode behavior, signal/no-signal DB evidence, unsupported variant blocking, and no-execution proof to Stage `06`. |
| `06` | Strategy compatibility checker + market-data readiness/provisioning для backtest variants. | accepted | None. Runtime API/SQL/Redis/browser/metrics evidence recorded after direct-main deploy; local workstation runtime CLIs/env were unavailable, so acceptance proof used Mac Studio production probes. | Передать accepted launchable/not_launchable/degraded reason codes, market-data ready/missing/stale/pending evidence, run/profile fail-closed behavior, and UI readiness semantics to Stage `07`. |
| `07` | Exchange account state projection + exchange configuration guard. | accepted | None. Runtime and public browser evidence recorded after manual app-image publish and web deploy from current `main`. | Передать accepted local projection schema, verify-only config guard, freshness/config-mismatch/degraded/stale reason codes, and the rule that safe account reads use trade-ready connections only; read-only keys remain rejected/not-ready. |
| `08` | Strategy position ownership lock. | accepted | None. Runtime API/SQL/Redis/metrics/browser evidence recorded after direct-main deploy; worker terminal release was proven with a one-iteration production live-runner invocation because the long-running `:9203` metrics endpoint is not open in current Mac Studio ops state. | Передать ownership schema, unique active invariant, conflict/release/stale-repair behavior, and no-order/no-execution-stream proof to Stage `09`. |
| `09` | Capital reservation + paper execution + strategy-local accounting. | accepted | None. Runtime API/SQL/Redis/metrics/browser evidence recorded after direct-main deploy; production smoke found and repaired the capital-reservation blocked HTTP status mapping before acceptance. | Передать accepted capital reservation + paper orders/fills/accounting schema, fee/funding model, idempotency, UI reserved budget/PnL/position/completeness evidence, and no-real-submit proof to Stage `10`. |
| `10` | Universal `ExecutionSourceEvent` + `ExecutionRequest` contract, explicit v1 order model, source registry, `ExecutionIntent` persistence. | accepted | None. Runtime API/SQL/Redis/metrics evidence recorded after direct-main deploy; first duplicate probe used a different key and intentionally produced another row, then corrected duplicate-only proof showed exact-key replay returns existing source/intent ids. | Передать accepted source-event/intent schema, SHA-256 idempotency semantics, `market/limit` allowlist, advanced-order rejection and no Redis/order submit proof to Stage `11`. |
| `11` | Source-aware authorization и risk gate v1. | accepted | None. Runtime API/SQL/Redis/metrics evidence recorded after direct-main deploy; initial CI static lint failed on Alembic line length and was repaired before acceptance. | Передать accepted/rejected reason codes, audit events, metrics и compatibility/market-data/account-config/account-state/capital-reservation/order-model/ownership/accounting/source/profile/run policy. |
| `12` | Redis dispatch transport after accepted durable intent. | accepted | None. Runtime API/SQL/Redis/metrics evidence recorded after direct-main deploy; local workstation Redis/Postgres services and `tests/integration` path were unavailable, so acceptance proof used Mac Studio production probes plus unit/apps/full-suite local gates. | Передать accepted stream names `execution.requests.v1`, `execution.requests.retry.v1`, `execution.requests.dlq.v1`, consumer group `exchange-execution.v1`, retry budget, backpressure retry, Redis outage/recovery, DLQ/quarantine and ack-after-durable rule. |
| `13` | `exchange-execution` supervised process skeleton. | accepted | None. Runtime launchd/Monit/Prometheus/API/DB/Redis evidence recorded after direct-main deploy and native-service installer/reload follow-ups. | Передать accepted supervised process boundary, local-only health/metrics/run-once API, disabled-adapter degraded readiness, heartbeat/observation persistence, Redis group/DLQ visibility, and no-submit/no-decrypt proof. |
| `14` | Native Binance/Bybit testnet order adapters + private stream lifecycle skeleton. | accepted | None. Implementation, CI and deploy are complete. Funded Bybit spot testnet proof recorded real `exchange-execution` submit/status/cancel/private-stream evidence with durable `execution_orders`, private stream readiness, Redis ack-after-durable state and bounded metrics. Binance futures testnet still needs credential rotation away from `stage03_no_decrypt_placeholder` for Binance-specific proof, but this is not a Stage 14 blocker because Bybit satisfied the native testnet adapter criterion. | Передать accepted Bybit spot testnet submit/status/cancel/private-stream path, mainnet hard-block proof, Redis ack-after-durable order decision, bounded metrics, and the limitation that Stage `15` still must implement fills/funding/reconciliation/PITR rather than treating submit/cancel as full ledger convergence. |
| `15` | Order/fill/funding ledger, private status updates, reconciliation, retention/partitioning, backup/PITR proof. | accepted | None. Direct-main implementation, CI/deploy, Mac Studio readiness, real Bybit spot testnet fill/status/cancel reconciliation, append-only order-event/fill/reconciliation rows, retention metadata, PITR restore drill and secret-clean Redis stream scan are complete. Literal `tests/integration` path is absent in this checkout, matching prior stage limitation. Futures funding facts remain a documented next-stage/runtime limitation, not a Stage 15 blocker for spot reconciliation. | Передать accepted order-event/fill/reconciliation ledger, `ledger_pitr=pitr_restore_verified` readiness, retention/PITR proof, Redis ack-after-durable reconciliation and the limitation that futures funding/PnL notifications must stay incomplete until real futures funding boundary proof exists. |
| `16` | Producer integrations: Strategy from backtest-created profile, Manual, ML agent contract, notification outbox. | accepted | None. Direct-main implementation, CI/deploy, Mac Studio smoke, API/SQL/Redis/metrics probes, and public `/strategies` browser proof are complete. Runtime proof used a synthetic strategy and temporary smoke session to cover `strategy_signal`, `manual_request`, `ml_agent_decision`, and `ops_test` through the same ingress/risk path; accepted ops dispatch wrote `execution.requests.v1`, rejected/no-intent paths did not dispatch, and outbox rows covered rejected, fill, unknown, kill-switch, and terminal notifications. | Stage `17` can start. Keep `ROEHUB_EXECUTION_STRATEGY_PRODUCER_ENABLED=0` until a later stage explicitly enables live Strategy producer writes in production; use the accepted source-event/intent/outbox links as the producer handoff surface. |
| `17` | Production readiness: latency/slippage, canary protocol, deploy/ops proof. | blocked | Production ops assets and pending self-heal are deployed, but a fresh single safe journey from backtest variant through Playwright-visible strategy outcome, source event, risk, Redis, testnet fill, reconciliation, notification, latency and slippage was not completed. Current `cancel_after_submit=true` market-fill behavior is known to create a cancel-after-fill adapter error and pending reconciliation row; no separate approved canary contract allowed disabling that for the fill smoke. | Keep commits `dac40caa` and `88ef1fe0`; approve or implement a bounded testnet fill canary that avoids the known cancel-after-fill pending state, then rerun full Playwright/API/DB/Redis/exchange/metrics evidence before accepting Stage `17`. |

## Обязательные Доказательства По Stage

| Stage | API/runtime | DB | Redis | Ops/metrics | Browser/exchange |
|---|---|---|---|---|---|
| `01` | `curl` текущих backtest/strategy/exchange API показывает наличие run/stop и отсутствие launch/restart/execution submit path. | SQL inventory показывает существующие backtest/strategy/exchange/binding tables и отсутствие profile/execution tables. | `XINFO` текущих market/strategy streams и отсутствие execution streams. | `monit summary`, inventory текущих `/metrics`. | Browser inventory для backtests/strategies/settings, если runtime доступен. |
| `02` | Реальные `GET variant` и `POST create strategy from variant`, duplicate idempotency, forbidden/not-launchable cases. | Strategy/provenance rows доказывают source job/variant/hash и отсутствие duplicate strategy при replay. | Не требуется. | `strategy_variant_launch_total`, audit rows. | Playwright proof: `/backtests` visible variant -> create live strategy; rejected variant shows safe reason. |
| `03` | Реальные profile create/update/readiness calls, включая live-mode recent-auth failure/success. | Profile rows доказывают mode/status/readiness/reason и отсутствие secrets. | Не требуется. | Profile readiness/audit metrics. | Playwright proof: `/strategies` live profile panel, safe default, blocked reason. |
| `04` | Реальные `run`, `stop`, `restart`, duplicate restart, active/stopping conflict. | `strategy_runs` и operation/event rows доказывают one active run max и restart sequence. | Evidence, что strategy worker подхватил run; execution stream отсутствует. | Run command/restart metrics. | Playwright user flow, нет duplicate active run после refresh. |
| `05` | Controlled closed candles создают warmup -> signal/no-signal через live runner. | `strategy_signals`/events доказывают mode behavior и блокировку unsupported variants. | Market-data stream input и strategy realtime output evidence. | Signal/evaluator metrics. | Playwright/API latest signal/journal; exchange calls отсутствуют. |
| `06` | Compatibility + market-data readiness calls доказывают launchable/not_launchable/degraded/forbidden/replay and ready/missing/stale/pending feed. | Compatibility rows and market-data requirement rows доказывают variant/profile hash, feed status, result, stable reason codes. | `XINFO` ready/stale/missing `md.candles...`; execution stream отсутствует. | Compatibility + market-data readiness counters and audit rows. | Playwright proof: `/backtests` and `/strategies` show safe launchability/feed reasons. |
| `07` | Read-only account sync/config guard call/worker доказывает fresh/stale/degraded/config-mismatch projection. | Account/balance/position/open-order/filter snapshots без secrets, с age/source hash/config requirement. | Optional stream reconnect evidence if used. | Account sync/config guard counters, staleness gauge, Monit/health if separate process. | UI/API readiness shows stale/fresh/config mismatch reason; no exchange submit/auto-config. |
| `08` | Two strategy/profile run attempts на один connection+instrument доказывают first owner and second conflict; release after stop. | `strategy_position_ownership` rows prove reserved/active/releasing/released/stale states and unique active invariant. | Не требуется. | Ownership counters and stuck alert query. | Playwright conflict reason, no unsafe override. |
| `09` | Reservation create/release and controlled signal in paper mode creates paper order/fill/accounting; replay idempotent; fee/funding status explicit. | Capital reservation + paper orders/fills/accounting snapshots prove reserved budget/position/equity/PnL and fee/funding assumptions. | Existing market/strategy streams only; execution dispatch empty unless shadowed. | Capital reservation + paper/accounting/fee-funding metrics. | Playwright reserved budget + paper position/PnL completeness, real exchange calls отсутствуют. |
| `10` | Реальные `POST` calls создают valid/invalid/duplicate source events и intents для всех source types; unsupported order model rejected. | `execution_source_events` и `execution_intents` rows доказывают status, source refs, source-event links, `strategy_signal_id`, order model fields, idempotency. | Dispatch stream отсутствует или пустой по контракту stage. | Source-event + intent + order-model-rejected counters and audit rows. | Не требуется. |
| `11` | Реальные accepted/rejected calls для incompatible variant, missing/stale feed, config mismatch, stale account projection, insufficient capital, unsupported order model, ownership conflict, inactive connection, profile blocked, run inactive, missing binding, no recent-auth, ML without policy, kill switch. | Durable risk status/reason and audit rows. | Rejected intents never dispatched. | Risk latency and result counters. | Не требуется. |
| `12` | Accepted intent возвращает dispatch state; retry budget и poison-message cases проверены фактическими сообщениями. | Status transitions `accepted/dispatching/dispatched/retry/quarantined` persisted. | `XINFO`, `XREADGROUP`, `XPENDING`, retry stream, DLQ stream, duplicate replay, outage recovery. | Dispatch, retry, DLQ и Redis error counters. | Не требуется. |
| `13` | Local `GET /health/ready` доказывает dependency, limiter/backpressure и clock drift states. | Process heartbeat/lock rows, если реализованы. | Consumer group и DLQ visible; submit calls отсутствуют. | launchd/Monit/Prometheus/rate/clock/DLQ evidence. | Не требуется. |
| `14` | Safe internal submit/cancel/status command через `exchange-execution` with limiter/config/time/order-model guards and private stream session/keepalive evidence. | `execution_orders` persisted with exchange order id/status and guard decisions. | Request acked только после DB update; retry/DLQ evidence for adapter errors. | Submit latency/counters, rate-limit wait, clock drift, private-stream metrics, secret grep clean. | Binance/Bybit testnet ack/cancel/status/private-stream session; mainnet blocked. |
| `15` | Restart/gap scenario запускает reconciliation, private stream reconnect/backfill, fee/funding reconciliation and retention/partition/PITR dry-run. | `execution_order_events`, `execution_fills`, `execution_funding_events`, `execution_reconciliation_runs` converge; partitions/indexes/retention/PITR metadata verified. | Pending messages reconcile with ledger. | Reconciliation, private-stream, partition/index-bloat, backup/PITR and alert evidence. | Testnet exchange state/fill/funding facts match local ledger. |
| `16` | Backtest-created Strategy/manual/ML producer calls создают source events и eligible intents через один ingress; notification outbox writes terminal/rejected/unknown events. | Source refs, `source_event_id`, intent links, outbox rows и no-intent outcomes доказывают producer-specific paths. | Accepted safe intents dispatch через тот же stream. | Per-source signal-to-intent/fill gap and notification metrics with bounded labels. | Playwright signal -> source event -> execution outcome + notification link; ML shadow/testnet proof. |
| `17` | Full E2E smoke plus rollback/kill-switch commands. | Terminal lifecycle rows match expected results от variant до fill/funding/reconciliation/notification; partition/retention/PITR proof recorded. | No stuck pending/DLQ messages. | Prometheus scrape, Monit restart/health, alert rules with severity/owner/escalation, clock, private-stream, rate limiter, retention/PITR proof. | Full Playwright journey + API proof + exchange testnet proof + latency/slippage/notification report. |

## Handoff Facts

| Факт | Значение |
|---|---|
| Backtest launch policy | Live strategy может создаваться только из owner-scoped launchable backtest variant; provenance обязателен. |
| Strategy/profile split | `StrategySpecV1` описывает логику, `LiveStrategyProfile` описывает mode/binding/sizing/limits/readiness. |
| Safe launch default | Backtest-created strategy не стартует live/mainnet неявно; default mode `monitor_only` или `paper`. |
| Restart policy | Restart является durable command, а не UI alias на stop+run; one-active-run invariant обязателен. |
| Signal evaluator policy | Live runner должен доказать `StrategySignal`/no-signal output на controlled candles до integration with execution. |
| Compatibility policy | Stage `06` обязан доказать `launchable`, `not_launchable`, `degraded` и owner-forbidden cases до дальнейших live/paper stages. |
| Market-data readiness policy | Stage `06` обязан доказать ready/missing/stale/pending feed и блокировать run до active/fresh runtime stream. |
| Account projection policy | Stage `07` создает локальную fresh/stale projection для readiness/risk/sizing; hot path не делает exchange read. Safe account reads go through exchange-control and require a trade-ready connection. |
| Exchange config guard policy | Stage `07`/`14` только проверяют leverage/margin/position mode/filters; auto-config запрещен до отдельного stage. Read-only exchange keys are not supported as usable readiness inputs. |
| Position ownership policy | Stage `08` запрещает двум active strategies владеть одним `exchange_connection_id + market_type + instrument_key`. |
| Capital reservation policy | Stage `09` обязан доказать durable reservation/release и insufficient/stale capital блокировку до paper/live. |
| Paper accounting policy | Stage `09` должен писать durable paper orders/fills/accounting; paper success без accounting и explicit fee/funding status запрещен. |
| Order model policy | Stage `10` фиксирует v1 allowlist `market|limit`; Stage `11`/`14` rejects OCO/TP/SL/trailing/amend/multi-leg до Redis/submit. |
| UI current-state policy | `/backtests` уже имеет results/variant detail surface, `/strategies` уже имеет selected strategy + run/stop; create-from-variant, live profile, restart, signal journal, compatibility/market-data/account state/ownership/capital/paper accounting и execution links должны добавляться в stages `02-09` и `16-17`. |
| Execution layer ownership | `exchange-execution` является producer-neutral money boundary, а не частью Strategy. |
| Источники v1 | `strategy_signal`, `manual_request`, `ml_agent_decision`, `ops_test`. |
| Redis роль | Fast transport only; source of truth для денег остается Postgres ledger plus exchange reconciliation. |
| Redis DLQ policy | Poison messages never submit orders; Stage `12`/`13` must prove quarantine and ledger-backed repair path. |
| Signal journal policy | `execution_source_events` records every factual strategy/manual/ML/ops signal or decision, including no-intent and rejected paths. |
| Mainnet policy | Mainnet order submit запрещен до отдельного canary approval после `17`. |
| ML policy | ML agent не получает прямой доступ к exchange SDK/API/secrets; он записывает `ExecutionSourceEvent`, а eligible decision создает `ExecutionRequest` и проходит risk gate. |
| Secrets policy | Producers и `apps/api` не decrypt credentials; secret-bearing operations scoped to approved exchange-control/exchange-execution path. |
| Runtime config policy | Все execution flags default-deny; producer flags и mainnet flag должны быть включены явно. |
| Latency policy | Hot path не делает внешнюю validation; latency/slippage принимаются только по runtime metrics, не по предположениям. |
| Clock policy | Stage `13`/`17` must prove clock drift below configured threshold before exchange submit readiness. |
| Private stream policy | Stage `14`/`15` must prove Binance/Bybit private stream session, keepalive, reconnect, backfill and dedupe behavior. |
| Retention policy | Stage `10`/`15`/`17` must prove partition/retention/archive design before production readiness. |
| Backup/PITR policy | Stage `15`/`17` must prove backup/PITR restore for money ledger before production readiness/canary. |
| Notification policy | Stage `16`/`17` must prove redacted notification outbox for rejected/fill/unknown/kill-switch states. |
| Alert ownership policy | Stage `17` alert rules/runbooks must include severity, owner and escalation path. |
| Documentation policy | Каждый stage обязан обновлять затронутые architecture/runbook/ops docs из основного плана или явно писать, почему doc не менялся. |
| File-map policy | Если исполнитель отклоняется от planned file map, причина и новый путь фиксируются в stage report и этом ledger. |

## Documentation And File Coverage

| Stage | Must consider docs | Must consider file areas |
|---|---|---|
| `01` | Main plan, этот ledger, related strategy/backtest/identity docs, `docs/architecture/README.md`. | No code changes; inventory only. |
| `02` | Backtest result docs, strategy API/domain docs, main plan, ledger. | Backtest variant lookup/DTO, create-from-variant use case, API route/DTO, backtests UI CTA/modal, locales, provenance events, optional migration. |
| `03` | Strategy API/domain docs, identity exchange connection doc, main plan. | `strategy_live_profiles` domain/repo/migration/API, profile validation/readiness, strategy/profile UI surface, metrics/audit. |
| `04` | Strategy API/run-control docs, live runner docs, strategy realtime docs. | Restart use case/API, optional run operation storage, worker stop/drain behavior, strategy UI action/state, run metrics/audit. |
| `05` | Strategy live runner docs, strategy realtime docs, main plan signal sections. | Signal evaluator, `StrategySignal` journal/events, live runner integration, market-data controlled input evidence. |
| `06` | Backtest result docs, strategy API/domain docs, market-data live-feed doc, main plan. | Compatibility checker service/use case/API DTO, reason codes, persisted checks, market-data readiness/provisioning port, `market_data_subscription_requirements`, `/backtests` and `/strategies` UI readiness. |
| `07` | Identity exchange connection doc, exchange secret runbook, operations doc if worker created, main plan. | Account projection domain/read models/migrations, native read-only adapters, exchange config guard, config requirement/readiness codes, sync worker/health, metrics/audit, stale/config-mismatch policy. |
| `08` | Strategy API/domain docs, strategy live worker runbook, main plan. | Position ownership domain/use cases/repo/migration, profile/run readiness checks, release/repair behavior, UI conflict reason. |
| `09` | Strategy live runner docs, strategy realtime docs, identity exchange doc, main plan capital/paper/fee-funding sections. | Capital reservation domain/use cases/repo/migration, paper execution/accounting domain/use cases/repo/migrations, fee/funding config/read model, live runner integration, UI reserved capital/paper position/PnL/completeness, metrics/audit. |
| `10` | Main plan, ledger, retention/partition note if separate. | `src/trading/contexts/live_execution/`, explicit `ExecutionRequest` order model DTO/value objects, migrations with `execution_source_events` + `execution_intents`, partition/retention scaffolding, APIs/wiring. |
| `11` | Main plan risk/error sections, identity exchange connection doc if usage guard changes. | Risk gate checks for compatibility/market-data/account-config/account-state/capital-reservation/order-model/ownership/accounting/profile/run/source policy, audit/metrics wiring. |
| `12` | Redis transport section, new or existing Redis runbook section. | Redis adapter/config, retry stream, DLQ/quarantine, retry budget metadata, `configs/{dev,test,prod}/`. |
| `13` | Operations architecture doc, new `docs/runbooks/exchange-execution.md`. | `apps/exchange_execution/`, rate limiter/backpressure/DLQ/clock drift guards, launchd, Monit, Prometheus scrape config. |
| `14` | Exchange secret runbook, identity exchange connection doc, exchange-execution runbook. | Native exchange adapters, limiter/config/time/order-model guard integration, private stream lifecycle skeleton, testnet config/env docs. |
| `15` | Main plan reconciliation/retention/backup section, exchange-execution runbook. | Postgres repos/migrations, reconciliation worker/use cases, exchange status/private stream adapters, fee/funding reconciliation, partition/retention/archive proof, backup/PITR restore proof. |
| `16` | Strategy live runner docs, strategy realtime docs, ML producer contract note, notification/outbox docs if separate. | Strategy producer port/adapter, manual UI/API if enabled, ML contract/stub, notification outbox rows/read model. |
| `17` | Operations doc, strategy/exchange runbooks, docs index. | Prometheus rules with severity/owner/escalation, Grafana dashboards, Monit, launchd, DLQ/clock/private-stream/PITR/retention/notification deploy verification artifacts. |

## Contract Impact Ledger

| Stage | Public/API | Persistence | Redis | Runtime/Ops | Rollback |
|---|---|---|---|---|---|
| `01` | none | none | none | none | docs-only inventory. |
| `02` | compatible-change: new create-from-backtest-variant API/DTO | additive provenance/event/profile seed if needed | none | metrics/audit additive | disable launch flag, keep created strategies as immutable audit records. |
| `03` | compatible-change: profile API/DTO | additive `strategy_live_profiles` or equivalent | none | metrics/audit additive | disable profile flag; profiles remain inert. |
| `04` | compatible-change: restart API/DTO; run/stop remain compatible | compatible-change: existing `strategy_runs.metadata_json` and `strategy_events` used; no new table | none: no execution streams | compatible-change: existing API/live-runner path gains restart drain behavior | revert Stage 04 commits; restart metadata/events remain inert audit data. |
| `05` | compatible-change: signal read/event surfaces if exposed | additive signal/event journal | existing market/strategy streams only | evaluator metrics additive | disable evaluator flag; runner returns to warmup/metrics-only behavior. |
| `06` | compatible-change: compatibility/readiness/feed API/DTO/reason codes | additive compatibility checks table or profile fields plus `market_data_subscription_requirements` if needed | existing market streams only; no execution streams | compatibility/feed metrics and audit additive | disable checker/readiness flags; profiles blocked rather than unsafe-ready. |
| `07` | compatible-change: readiness/risk can expose stale account/config mismatch reasons | additive account/balance/position/open-order/filter snapshots plus config requirement/result rows if needed | optional private stream reconnect only | account sync/config guard worker/metrics/alerts additive | disable sync/config guard flags; live/paper readiness blocks until projection/config evidence available. |
| `08` | compatible-change: run/profile may return ownership conflict | additive `strategy_position_ownership` | none | metrics/audit additive | disable ownership flag only before live; otherwise release/repair then disable. |
| `09` | compatible-change: capital reservation and paper accounting read surfaces with fee/funding completeness | additive `strategy_capital_reservations` plus paper orders/fills/accounting tables and fee/funding metadata | existing market/strategy streams only | capital reservation + paper/accounting/fee-funding metrics additive | disable capital/paper flags only after release/stop path is verified; data remains audit. |
| `10` | compatible-change: new execution source-event/intent API/DTO with explicit v1 order model | additive `execution_source_events` + `execution_intents` with order model fields and partition/retention scaffolding | none | source/intent/order-model rejection metrics and audit additive | disable routes/config, keep tables inert. |
| `11` | compatible-change: risk outcomes/reasons include compatibility/feed/config/capital/order-model/ownership/source | additive risk fields/audit | none or accepted-only publish hook | risk metrics additive | feature-flag risk gate to reject all non-test intents. |
| `12` | compatible-change: dispatch state is observable | status/retry/quarantine fields used | new execution, retry and DLQ streams/groups | Redis dependency for dispatch plus retry/DLQ metrics | disable dispatch worker, leave intents accepted/not submitted; DLQ is repair-only. |
| `13` | none/public; internal health only | optional heartbeat/locks | consumer group and DLQ group | new supervised process with limiter/backpressure/clock guards | stop process and disable launchd/Monit entry; pending intents remain durable. |
| `14` | internal adapter contract | orders table used with guard decisions | request consumption plus retry/DLQ on adapter errors | exchange testnet dependency, rate/config/time/order-model/private-stream guards | disable adapters/private streams; keep intents/orders for audit. |
| `15` | event/status query compatibility may be additive | fills/funding/events/reconciliation tables plus partitions/retention/PITR metadata used | event/pending streams reconcile with ledger | reconciliation, retention and backup/PITR proof jobs/processes | pause reconciliation/retention jobs; ledger remains durable and restore evidence blocks readiness if missing. |
| `16` | producer integration endpoints/ports and notification read surfaces additive | source refs, source-event links and notification outbox rows used | same dispatch stream for accepted safe intents | producer signal-gap and notification metrics additive | disable producer and notification feature flags independently. |
| `17` | no new contract unless canary approved | no destructive migration; partition/retention/PITR verified before activation | no destructive change; DLQ must be empty or explained | deploy/monitoring config active with severity/owner/escalation alerts for clock/rate/DLQ/private-stream/PITR/retention/notifications | kill switch, stop process, revert config/commit if needed. |

## Stage Evidence Log

| Stage | Status | Evidence summary | Commands / evidence refs | Commit / deploy | Notes |
|---|---|---|---|---|---|
| `01` | accepted | Static inventory found current backtest result/variant routes, strategy CRUD/clone/run/stop, strategy live-runner, exchange connections, strategy exchange bindings, and ops config. Mac Studio runtime evidence confirmed API health/openapi, current authenticated routes returning `401`, missing restart/execution routes returning `404`, SQL table presence/absence, Redis market-data streams with no execution streams, exchange-control ready/metrics, launchd services, Monit OK, and Prometheus `up=1` for core targets. Authenticated browser inventory confirmed `/backtests`, `/strategies`, and `/settings` current UI surfaces and absence of create-from-variant, restart, live profile, signal journal, execution console, and order-submit controls. `backtest-artifact-publisher` remains excluded from automatic startup per Mac Studio runbook; no service start was performed. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/01-current-state-baseline.md`; SSH `macstudio` API/openapi/route probes; SQL metadata query; Redis scan/XINFO; exchange-control `/health/ready` and `/metrics`; launchd/Monit/Prometheus inventory; authenticated Playwright browser inventory. | Direct-main delivery completed before Stage `02`. | Docs-only change; no product code/schema/config/runtime changes. |
| `02` | accepted | Added owner-scoped create-from-backtest-variant use-case, API route, UI CTA/modal, provenance table and bounded metric. Post-deploy synthetic runtime proof on Mac Studio showed `GET variant=200`, `POST create=201`, replay `200 duplicate=true`, forbidden `403`, missing idempotency `422`, missing variant `404`, not-launchable `409`, SQL `provenance_rows=1`, `distinct_strategy_rows=1`, `event_rows=1`, `run_rows=0`, SHA-256 idempotency hash shape, and bounded `strategy_variant_launch_total` labels. Browser proof on `https://roehub.com/backtests` showed authenticated Results tab, variant detail, create modal with no-live/mainnet wording, visible `409 not_launchable`, and launchable submit `POST .../strategies => 200` duplicate replay. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/02-backtest-variant-strategy-creation.md`; local gates: `uv run ruff check src/trading/contexts/backtest src/trading/contexts/strategy apps/api tests`, `uv run pyright src/trading/contexts/backtest src/trading/contexts/strategy apps/api tests`, `uv run pytest -q tests/unit/contexts/backtest tests/unit/contexts/strategy tests/unit/apps`; deploy repair gates: `uv run pytest -q tests/unit/apps/migrations/test_identity_exchange_audit_events_sql.py tests/unit/apps/migrations/test_strategy_exchange_bindings_sql.py`; runtime: Mac Studio smoke, API/SQL/metrics synthetic probe, Playwright browser flow. | `ca69d631` implementation, `8ba38ae8` identity audit bootstrap replay deploy repair, `0e3a9b07` metric reason sanitization. CI `26693054391`, `26693175577`, `26693431678` successful; backend deploys `26693216924` and `26693505333` successful; app image and web deploys successful. | Stage `03` may start from accepted facts: created strategy is immutable and inert, provenance is durable, replay does not create a second strategy, and no profile/run/order/exchange submit exists in Stage `02`. |
| `03` | accepted | Added `LiveStrategyProfile` domain/service/repositories, additive `strategy_live_profiles` Alembic storage, profile API endpoints, non-secret exchange-control readiness checker, bounded readiness metric, and `/strategies` live profile panel. Local and post-deploy runtime proof showed default `monitor_only`, `paper` ready, `live` without recent auth blocked as `recent_auth_required`, fresh-session live ready only with eligible exchange-control connection, readiness refresh, dashboard read-model profile, metrics, redaction, SQL profile/event rows, and public browser panel on desktop/mobile. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/03-live-strategy-profile.md`; local gates: `uv run ruff check .`, `uv run pyright`, `uv run pytest -q -ra`, `python -m tools.docs.generate_docs_index --check`, `git diff --check`; runtime: Mac Studio smoke, API/SQL/metrics synthetic probe, Playwright public browser screenshots under `output/playwright/`. | `6a3ea338` implementation. CI `26694694133` success; Deploy Backend `26694731561` success; Publish App Image `26694731562` success; Deploy Web `26694731541` success and rerun `26694755752` attempt `2` success after transient first-attempt edge smoke 502. | No Redis, decrypt, exchange submit, paper accounting, position ownership or capital reservation exists yet. |
| `04` | accepted | Added durable restart use-case/API, UI restart control, live-runner stop/drain successor behavior, and Postgres run metadata JSONB serialization repair. Post-deploy production proof showed restart without active `409`, run start `200`, duplicate run `409`, restart active `200 stopping` with operation id, duplicate restart `409`, run while stopping `409`, worker restart drain with `polled_runs=1`, successor stop `200`, worker stop drain with `polled_runs=1`, stop without active `409`, SQL old run `stopped/restart.drained`, SQL successor `stopped/restart.successor_started`, final active rows `0`, and event rows for `strategy_created`, `run_started`, `run_restart_requested`, `run_stop_requested`. Separate browser target had SQL active rows `1`, public `/strategies` showed `Run` disabled, `Stop` and `Restart` enabled, run state `starting`, manual refresh `200`, console clean, then cleanup stopped/drained the run and revoked the temporary session. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/04-run-stop-restart-lifecycle.md`; local gates: `uv run ruff check .`, `uv run pyright`, `uv run pytest -q -ra`, `uv run python -m tools.docs.generate_docs_index --check`, `git diff --check`; runtime: Mac Studio smoke, production API/SQL/worker synthetic probe, Redis key scan, Playwright public browser screenshots under `output/playwright/`. | `f1b5004a` implementation and `586a2742` production-found metadata persistence repair. CI `26695446121` and `26695742560` successful; Deploy Backend `26695484006` and `26695760206` successful; Publish App Image `26695484003` and `26695760210` successful; Deploy Web `26695514346`, `26695760229`, and `26695763634` successful. | No execution stream, exchange submit, credential decrypt, paper accounting, position ownership or capital reservation exists yet. Stage `05` can rely on accepted run/stop/restart semantics and worker drain behavior. |
| `05` | accepted | Added additive `strategy_signals` journal, Stage 05 `MA(fast,slow)` signal evaluator, live-runner persistence after closed buckets, `strategy_signal_total{mode,action,outcome}` metric, API/dashboard projection, and `/strategies` signal journal panel. Post-deploy production proof showed controlled Redis stream `md.candles.1m.codexstage0572d2ed:spot:BTCUSDT` with `XINFO length=4`, `XRANGE count=4`, pending `0`; live runner report `polled_runs=3`, `read_messages=4`, `acked_messages=4`, `failed_runs=1`; DB `strategy_signals` rows `9`; monitor-only `warmup,warmup,no_signal,signal`; paper signal reason `ma_fast_crossed_above_slow_paper_no_order_stage05`; unsupported variant `blocked/unsupported_live_evaluator` and failed run; `expected_order_json` non-empty rows `0`; Redis `*execution*` keys `0`; `execution_intents` table absent; realtime output streams written; API signal journal source `strategy_signals` ready with `4` rows; public `/strategies` Playwright proof rendered `4` signal rows and visible monitor-only no-intent reason. Temporary smoke session was revoked and active smoke runs cleaned to `0`. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/05-live-signal-evaluator-journal.md`; local gates: `uv run ruff check .`, `uv run pyright`, `uv run pytest -q -ra`, `uv run python -m tools.docs.generate_docs_index --check`, `git diff --check`; runtime: Mac Studio smoke, production Redis/SQL/API/metrics probe, Playwright screenshot `output/playwright/stage05-strategies-signals.png`. | `17c56b36` implementation. CI `26696418272` success; Deploy Backend `26696449721` success; Publish App Image `26696449720` success with non-fatal Docker cache reservation warning; Deploy Web `26696468892` success. | No execution intent, execution Redis stream, exchange credential decrypt, exchange SDK/API call, paper accounting, position ownership or capital reservation exists yet. Stage `06` must preserve fail-closed unsupported evaluator semantics and prevent unsupported variants from presenting as live-ready before run. |
| `06` | accepted | Added compatibility/readiness use-case, ports, Postgres/in-memory repositories, Redis market-data readiness reader, additive compatibility/readiness migration, run/profile fail-closed checks, API endpoints, dashboard/backtests/strategies UI fields, bounded metrics, and focused tests. Post-deploy production proof showed strategy API states `launchable/ready`, `launchable/missing`, `launchable/stale`, `launchable/pending`, `degraded/ready`, and `not_launchable/ready`; ready run returned `200 starting`, missing-feed run blocked with `strategy_run.readiness_blocked`; backtest variant readiness returned `not_launchable/unsupported_live_evaluator`, repeat read `200`, and foreign owner `403`; SQL recorded `10` compatibility rows and `10` market-data requirement rows for the smoke user; Redis proved ready/stale/pending/missing stream states and `*execution*` key count `0`; metrics emitted bounded compatibility/feed counters; Playwright proved `/strategies` and `/backtests/{job_id}` readiness display. Temporary smoke sessions were revoked and active smoke runs cleaned to `0`. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/06-compatibility-market-data-readiness.md`; local gates: `uv run ruff check .`, `uv run pyright`, `uv run pytest -q -ra`, `uv run python -m tools.docs.generate_docs_index --check`, `git diff --check`; runtime: Mac Studio smoke, production API/SQL/Redis/metrics probe, Playwright screenshots `output/playwright/stage06-strategies-readiness-rerun.png` and `output/playwright/stage06-backtests-readiness-selected.png`. | `2001e415` implementation, `50234af4` evidence update. CI `26697234121` and docs-only CI `26697584533` successful; Deploy Backend `26697309560` success; Publish App Image `26697309566` success with non-fatal Docker cache reservation warning; Deploy Web `26697309568` success; follow-up Deploy Web `26697329959` passed after rerun of a transient public-edge smoke failure. | No execution intent, execution Redis stream, exchange credential decrypt, exchange SDK/API call, account projection, paper accounting, position ownership or capital reservation exists yet. Stage `07` can rely on accepted compatibility/feed readiness reason codes and fail-closed run/profile behavior. |
| `07` | accepted | Added `live_execution` account projection/config guard domain, repository ports/adapters, exchange-control account-state read boundary, additive snapshot migration, bounded metrics helpers, diagnostic sync script and `/strategies` account readiness panel. Production proof on Mac Studio used the smoke account's Bybit trade-ready mainnet connection for safe account-state reads: `fresh/account_state_read_ok`, 9 balances, 0 positions, 0 open orders, 1 filter, 64-char source hash, verified guard. A high min-notional requirement proved `config_mismatch/min_notional_below_requirement`; a second strategy/profile proved dashboard `degraded/account_projection_missing`; waiting past the 2-minute policy proved `stale/account_projection_stale`. The smoke read-only Bybit key remained `valid_readonly` but `connection_readiness=rejected`, `read_only_not_supported`, `effective_capability=none`; sync against it failed closed with exchange-control `422 read_only_not_supported`. API and exchange-control metrics emitted fresh/config_mismatch/degraded/stale/rejected counters and staleness gauge updates. Local Playwright mock-browser proof rendered all four account states with no secret DOM terms. Public web was rebuilt manually after the image-route gap and Playwright on `https://roehub.com/strategies` showed the Account readiness panel with `stale: account_projection_stale` and `secretLeak=false`. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/07-exchange-account-projection-config-guard.md`; local gates: focused ruff/pyright/pytest, required `uv run ruff check src/trading/contexts/live_execution src/trading/contexts/identity apps tests scripts/live_execution/sync_exchange_account_projection.py`, required `uv run pyright src/trading/contexts/live_execution src/trading/contexts/identity src/trading/contexts/exchange_control apps tests`, required `uv run pytest -q tests/unit/contexts/live_execution tests/unit/contexts/identity tests/unit/contexts/exchange_control tests/unit/apps`, docs index and diff check; runtime: Mac Studio smoke, exchange-control internal account-state calls, SQL snapshot/guard queries, API dashboard reads, metrics scrapes, public and local Playwright screenshots under `output/playwright/`. | `7a1c0986`, `8830448a`, `4d1a0a57`, `cfc45915` pushed to `main`; CI `26714932523`, Deploy Backend `26714942468`, manual Publish App Image `26715014736`, and manual Deploy Web `26715048528` succeeded. Earlier manual Deploy Web `26714967019` failed because the requested image tag did not exist before manual publish completed; rerun after publish succeeded. | Stage `08` can start. It must use local projection/readiness state only, preserve read-only-key rejection, and keep no-order/no-auto-config behavior. |
| `08` | accepted | Added durable `strategy_position_ownership` lock with reserve/activate/releasing/released/stale-repair states, Postgres/in-memory repositories, Strategy run/stop/restart/live-runner integration, bounded ownership metrics, 409 API mapping, and `/strategies` action-status error rendering. Production proof on Mac Studio used two monitor-only strategies on the smoke account's Bybit trade-ready connection `8e3999ba-c35d-4bcc-8253-a12b1d458114` and `bybit:spot:BTCUSDT`: both were `launchable/ready`, first run returned `200 starting` and ownership `active`, second run returned `409 position_ownership_conflict`, diagnostic stale transition moved the owner to `stale_requires_repair` and still blocked the second run, stop returned `200 stopping` and ownership `releasing`, one production live-runner `run_once` reported `polled_runs=1`, `active_instruments=1`, `read_messages=0`, `acked_messages=0`, `failed_runs=0`, then SQL showed run `stopped` and ownership `released`. API metrics emitted reserved/active/conflict/releasing counters; Redis `*execution*` keys were `0`; `execution_intents` table was absent. Public Playwright on `https://roehub.com/strategies` rendered `Position ownership is already held by another active strategy run: position_ownership_conflict`, screenshot `output/playwright/stage08-prod-strategies-position-conflict.png`, no failed requests beyond the expected 409 and secret DOM scan `false`. Temporary smoke sessions were revoked; active sessions/runs/blocking ownership rows were all `0`. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/08-strategy-position-ownership-lock.md`; local gates: focused ruff/pyright/pytest, broader focused tests, `node --check apps/web/dist/js/pages/strategies.js`, `uv run python -m tools.docs.generate_docs_index --check`, `git diff --check`, required `uv run ruff check .`, required `uv run pyright`, required `uv run pytest -q -ra`; runtime: Mac Studio production API/SQL/Redis/metrics smoke, live-runner one-iteration terminal release, public Playwright screenshot under `output/playwright/`. | `ce1c52b2` pushed to `main`; CI `26716333983`, Deploy Backend `26716376148`, Publish App Image `26716376147`, Deploy Web `26716376154`, and follow-up Deploy Web `26716401645` succeeded. Publish App Image had a non-fatal Docker cache reservation annotation. | Stage `09` can start. Position ownership is necessary but not sufficient; Stage `09` still needs durable capital reservation, paper orders/fills and strategy-local accounting before paper/live execution can be accepted. |
| `09` | accepted | Added durable capital reservation and paper order/fill/accounting. Production proof showed stale projection and insufficient balance both blocked with HTTP `409 strategy_run.capital_reservation_blocked`; successful paper run reserved `100 USDT`; controlled Redis candles produced one paper buy signal, one paper order, one fill and one accounting snapshot with position `5`, entry `20`, equity `99.8`, fee `0.1`, funding `0`, `paper_fixed_bps_10`, `spot_not_applicable`, `pnl_complete=true`; replay kept order/fill counts at `1`; stop plus live-runner terminal iteration released the reservation; cleanup left active smoke runs/reservations `0`. Redis execution key scan returned `0` and `execution_intents` table was absent. Public `/strategies` Playwright rendered the Paper accounting panel with reserved budget, paper position, PnL, fee/funding model and completeness, screenshot `output/playwright/stage09-strategies-paper-accounting.png`. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/09-capital-reservation-paper-accounting.md`; local gates: focused ruff/pyright/pytest, API/web regression, `node --check apps/web/dist/js/pages/strategies.js`, docs index and diff check, required `uv run ruff check .`, required `uv run pyright`, required `uv run pytest -q -ra`; runtime: Mac Studio production API/SQL/Redis/metrics smoke, live-runner one-shot paper accounting and release proof, public Playwright screenshot under `output/playwright/`. | `72e4cc1b` implementation, `5642bb95` production-found HTTP mapping repair. CI `26717573957` and `26717822941` succeeded; Deploy Backend `26717617956` and `26717866018` succeeded; Publish App Image `26717617942` and `26717866012` succeeded; Deploy Web `26717617959`, `26717638407`, `26717866016`, and `26717869924` succeeded. Publish App Image had a non-fatal Docker cache reservation annotation. | Stage `10` can start. Stage `09` proves paper accounting only; no execution source event, execution intent, Redis dispatch, risk gate, real order submit or exchange fill exists yet. |
| `10` | accepted | Added producer-neutral `ExecutionSourceEvent`, `ExecutionRequest`, v1 `market/limit` order model validation, source registry for `strategy_signal`, `manual_request`, `ml_agent_decision`, `ops_test`, and durable `execution_intents` persistence. Production proof created source events and recorded intents for all four source types; invalid `strategy_signal` without `strategy_signal_id` returned `400 execution.invalid_source_event/strategy_signal_id_required`; unsupported TP/SL returned `422 execution.unsupported_order_model/tp_sl_not_supported`; exact duplicate source/intent replays returned HTTP `200`, `duplicate=true`, same ids; SQL proved source-event links, `strategy_signal_id`, status and idempotency hashes; Redis `*execution*` scan returned `0` and `execution.requests.v1` was absent; metrics exposed source event, intent and order-model rejection counters. Temporary smoke sessions were revoked to `0`. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/10-execution-source-intent-order-model.md`; local gates: focused ruff/pyright/pytest, required `uv run ruff check src/trading/contexts/live_execution apps tests`, `uv run pyright src/trading/contexts/live_execution apps tests`, `uv run pytest -q tests/unit/contexts/live_execution tests/unit/apps`, full `uv run pytest -q -ra`, docs index and diff check; runtime: Mac Studio production API/SQL/Redis/metrics probe. | `650e4dfb` pushed to `main`; CI `26718954140`, Publish App Image `26718993829`, Deploy Backend `26718993846`, Deploy Web `26718993847` and follow-up `26718997002` succeeded. | Stage `11` can start. Stage `10` records intents as `recorded` with `risk_status=not_evaluated` and never publishes Redis dispatch or submits orders. |
| `11` | accepted | Added producer-neutral source-aware risk gate, additive risk audit table and bounded risk metrics. Local API boundary proof created `15` intents and `15` risk audit events: `1` accepted, `14` rejected, with stable reasons for incompatible variant, missing/stale feed, config mismatch, stale account projection, insufficient capital, ownership conflict, inactive connection, profile blocked, run inactive, missing binding, no recent-auth, ML without policy and kill switch. Unsupported TP/SL still returns `422 tp_sl_not_supported`. Production proof on Mac Studio used real authenticated API calls, Postgres, Redis and `/metrics`: `15` API cases produced `1` accepted and `14` rejected persisted intents; `15` audit rows all had `dispatch=no-dispatch`; source events included `order_model_rejected/tp_sl_not_supported`; Redis `*execution*` key count was `0` and `execution.requests.v1` was absent; risk gate counter and latency metrics were present. Mac Studio smoke passed. Deviation: risk evaluation currently consumes bounded API risk context snapshots and fails closed as `risk_state_unavailable`; repository-backed automatic risk-state assembly remains next-stage/follow-up work before opening dispatch beyond controlled probes. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/11-source-aware-risk-gate.md`; local gates: focused tests, required `uv run ruff check src/trading/contexts/live_execution src/trading/contexts/strategy src/trading/contexts/identity apps tests`, required `uv run pyright src/trading/contexts/live_execution src/trading/contexts/strategy src/trading/contexts/identity apps tests`, required `uv run pytest -q tests/unit/contexts/live_execution tests/unit/contexts/strategy tests/unit/contexts/identity tests/unit/apps`, full `uv run pytest -q -ra`, docs index and diff check; runtime: local FastAPI boundary probe, Mac Studio production API/SQL/Redis/metrics probe. | `edc8e955` implementation, `089c895c` Alembic lint repair. CI `26719536539` initially failed static lint on Alembic `E501`; final CI `26719584962` succeeded. Publish App Image `26719665047`, Deploy Backend `26719665048` and Deploy Web `26719665044` succeeded. | Stage `12` must dispatch only `status=accepted` and `risk_status=accepted`. Rejected and legacy `recorded/not_evaluated` intents are not dispatchable. |
| `12` | accepted | Added Redis dispatch transport for durable accepted execution intents. Production API proof created an accepted intent that returned `status=dispatched`, Postgres recorded `dispatch_attempt_count=1`, `dispatch_stream_name=execution.requests.v1` and a Redis message id; exact duplicate replay returned `200 duplicate=true` with no second primary message; missing-risk rejected intent stayed `rejected/risk_state_unavailable` and had `0` primary stream messages. Redis proof showed `XINFO STREAM execution.requests.v1` length `1`, one `exchange-execution.v1` group, `XREADGROUP` read one message, `XPENDING` moved from `1` to `0` after `XACK`. Direct safe probes proved backpressure `retry/dispatch_backpressure` with one retry marker, Redis outage `retry/ConnectionError` followed by recovery to `dispatched/redis_xadd_ok`, and poison `dlq/stage12_poison_probe` with DB `quarantined` plus one DLQ marker. API `/metrics` exposed dispatch/retry/DLQ/backpressure/Redis-error counters; latest request/retry/DLQ stream entries had no secret-bearing terms. Temporary smoke session was revoked. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/12-redis-dispatch-transport.md`; local gates: focused Stage 12 tests, required `uv run ruff check src/trading/contexts/live_execution apps configs tests`, `uv run pyright src/trading/contexts/live_execution apps tests`, `uv run pytest -q tests/unit/contexts/live_execution tests/unit/apps`, full `uv run pytest -q -ra`, docs index and diff check; runtime: Mac Studio smoke plus production API/SQL/Redis/metrics probe. Literal required `tests/integration` path was absent. | `565f0ace` pushed to `main`; CI `26720381203`, Publish App Image `26720422220`, Deploy Backend `26720422250`, Deploy Web `26720422190` and follow-up `26720426619` succeeded. Evidence update follows in docs-only commit. | Stage `13` can start. It must consume `execution.requests.v1` through `exchange-execution.v1`, preserve Redis as transport only, update durable money-boundary state before `XACK`, and keep rejected/legacy/not-evaluated/quarantined rows non-dispatchable without manual repair. |
| `13` | accepted | Added separate `apps/exchange_execution` FastAPI process with `/health`, `/health/ready`, `/metrics`, local internal `run-once`, disabled-adapter-only config, heartbeat and request-observation persistence, Redis request observation through `execution.requests.v1` / `exchange-execution.v1`, poison/non-dispatchable durable observation plus DLQ marker and ack-after-observation, bounded readiness/Redis/DLQ/ack/clock metrics, launchd/Monit/Prometheus assets, and runbook. Local proof passed focused tests, required ruff/pyright/unit-app scopes, full `uv run ruff check .`, full `uv run pyright`, full `uv run pytest -q -ra` (`1080 passed`), docs index, diff check, local app smoke, shell syntax, and native service regression tests. Literal required `tests/integration` path was absent, matching Stage `12` local limitation. Mac Studio proof showed LaunchAgent installed and loaded, Monit `roehub_exchange_execution OK`, `/health` ok, `/health/ready` HTTP `200` with `degraded/adapter_disabled_stage13`, Redis/backpressure/DLQ/clock/Postgres ready, internal `run-once` no-submit with zero reads/acks, Prometheus target `up=1`, Postgres heartbeat/observation counts `1|1`, latest heartbeat `exchange-execution|degraded|adapter_disabled_stage13`, and `smoke_prod.sh` success. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/13-exchange-execution-process-skeleton.md`; local gates: focused Stage 13 tests, required `uv run ruff check apps/exchange_execution src/trading/contexts/live_execution tests`, required `uv run pyright apps/exchange_execution src/trading/contexts/live_execution tests`, required `uv run pytest -q tests/unit/contexts/live_execution tests/unit/apps`, full suite, docs index, native service script/regression checks; runtime: Mac Studio launchd/Monit/health/ready/run-once/metrics/Prometheus/Postgres/smoke proof. | `35255ae5` implementation, `b12651f6` native service installer/reload repair, `488db042` Monit reload workflow repair. CI `26722743278`, `26722919238`, and full matrix CI `26723039066` succeeded; Publish App Image `26722819985`, `26722959212`, and `26723120694` succeeded; Deploy Backend `26722819989`, `26722959240`, `26723120686`, and workflow-dispatch `26723143386` succeeded; Deploy Web `26722819995`, `26722824151`, `26722959214`, `26722963442`, and `26723120684` succeeded. | Stage `14` can start. It must add testnet-only native Binance/Bybit adapters, exchange-execution-scoped credential resolution, exchange server-time checks, limiter integration, private stream lifecycle skeleton, and mainnet hard-block evidence before any submit path is accepted. |
| `14` | accepted | Added testnet-only native Binance/Bybit adapter implementation, durable `execution_orders` and `exchange_private_stream_sessions` schema, exchange-control credential resolver, server-time/mainnet/config guards, private stream session skeleton, bounded adapter metrics and focused tests. Runtime reached `exchange-execution` on Mac Studio. Historical blockers were recorded for undecryptable Binance placeholder credentials and unfunded Bybit spot/linear probes. After the Bybit spot testnet trading balance was funded, exchange-control account-state sync returned `fresh/account_state_read_ok`, `balance_count=1`, a usable USDT bucket, `filter_count=1`, and `min_notional=5`; controlled Bybit spot dispatch returned `run-once` HTTP `200`, `submitted_count=1`, `acked_count=1`, `adapter_error_count=0`; `execution_orders` recorded `cancelled/cancel_requested` with provider order id present, submit/status/cancel timestamps present and adapter attempt count `1`; private stream session state was `ready/private_ws_auth_probe_ready`; final observation was `testnet_submitted/testnet_submit_status_cancel_recorded`; Redis pending stayed at the pre-existing `1`; `/health/ready` stayed `ready/all_dependencies_ready`; metrics included Bybit submitted order, ack, observation, private stream, latency and clock-drift samples. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/14-native-testnet-order-adapters.md`; local gates include focused and full ruff/pyright/pytest, docs index and diff check; runtime evidence includes Alembic `20260531_0028`, ready `/health/ready`, schema proof, Binance credential-boundary guard, earlier Bybit insufficient-balance adapter errors, temporary futures connection archived, mainnet hard-block, funded Bybit submit/status/cancel, private stream readiness, Redis ack-after-durable guard/error/submit decisions and bounded metrics. | Implementation commit `c2d3dd28`, guard-row repair `58810670`, blocker report `5a9888f6`, Bybit balance blocker report `73540642`, and this final acceptance report pushed to `main`; CI `26723973362`, `26724194507`, and `26724361968` succeeded; Publish App Image `26724051166`/`26724237547`/`26724370683`, Deploy Backend `26724051155`/`26724237558`/`26724370694`, and Deploy Web `26724051152`/`26724055221`/`26724237553`/`26724370742`/`26724374809` succeeded. | Stage `15` can start. It must implement order/fill/funding reconciliation, private stream reconnect/backfill, terminal state convergence, retention/partitioning and PITR proof before exchange-facing execution can be considered fully reconciled. |
| `15` | accepted | Added append-only `execution_order_events`, `execution_fills`, `execution_funding_events`, `execution_reconciliation_runs`, `execution_ledger_retention_policies` and `execution_ledger_pitr_drills`, extended native status adapters with normalized fill facts, added reconciliation metrics and fail-closed PITR readiness. Mac Studio proof after deploy showed `/health/ready` `ready/all_dependencies_ready`, `ledger_pitr=pitr_restore_verified`, Alembic `20260602_0029`, ledger tables present, counts `execution_order_events=10`, `execution_fills=1`, `execution_reconciliation_runs=3`, retention policies `5`, PITR drills `1`. Controlled Bybit spot testnet market probe `stage15-market-fill-probe-20260603-03` produced one provider fill and `matched/spot_order_status_and_fills_matched`; the post-fill cancel returned `exchange_ret_code_170213` and was recorded as `pending/adapter_error_reconciliation_pending`. Controlled low limit probe `stage15-limit-cancel-probe-20260603-01` returned `submitted_count=1`, `adapter_error_count=0`, `acked_count=1`, DB order `cancelled/cancel_requested`, and `matched/spot_order_status_matched`. Retention policies are `2555` days with archive-before-purge and PITR required. PITR was verified by `pg_dump` / `pg_restore` into an isolated temporary local Postgres cluster on Mac Studio and recorded as `verified/stage15_temp_cluster_restore_verified`. Redis recent request/retry/DLQ scan had `0` secret-like term hits; metrics exposed ready dependency, Bybit submit/private-stream counters and reconciliation counters. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/15-order-fill-reconciliation-pitr.md`; local gates include focused ruff/pyright/pytest, required ruff/pyright/unit-app scopes, full `uv run ruff check .`, full `uv run pyright`, full `uv run pytest -q -ra` (`1091 passed, 3 warnings`), docs index and diff check. Runtime evidence includes Mac Studio smoke, `/health/ready`, `/metrics`, Alembic/schema/count SQL, real Bybit spot testnet market fill and limit cancel probes, Redis stream redaction scan, retention policy rows and PITR temporary-cluster restore row. | Implementation commit `4c2fa626` pushed to `main`; CI `26848950859`, Publish App Image `26849114751`, Deploy Backend `26849114746`, Deploy Web `26849114759` and follow-up Deploy Web `26849126853` succeeded. Evidence update follows in docs-only commit. | Stage `16` can start. It must keep producers on the single ingress/risk/dispatch/exchange-execution ledger path, use Stage 15 order/fill/reconciliation state for notifications, and preserve the documented limitation that futures funding facts are not yet proven by a real provider boundary. |
| `16` | accepted | Added redacted `execution_notification_outbox`, widened source-event outcomes, Strategy producer source-event port and ACL adapter, rejected/kill-switch/fill/unknown/terminal notification writes, additive `/ui/execution/notifications`, bounded notification metrics, exchange-execution notification hooks, and `/strategies` signal/source/intent/execution/notification outcome panel. Mac Studio proof after deploy showed `/opt/roehub/app` contains Stage 16 files, `smoke_prod.sh` passed, Alembic `20260603_0030`, and tables `execution_notification_outbox`, `execution_source_events`, `execution_intents` exist. Runtime marker `stage16-20260602T222018-d29a99c2` created synthetic strategy `d6a4633a-1c14-4cc9-8a53-4c8b1693485d`: Strategy ACL adapter recorded `strategy_signal/no_intent`, strategy kill-switch was `risk_rejected/kill_switch_closed`, manual was `risk_rejected/manual_recent_auth_required`, ML was `risk_rejected/ml_agent_policy_missing` with explicit `producer_unknown`, ops accepted intent dispatched to `execution.requests.v1` and Redis stream length moved `12 -> 13`, and terminal ops notification remained no-intent/no-dispatch. SQL by strategy id proved six source-event rows and outbox rows for `producer_fill`, `producer_kill_switch`, `producer_rejected`, `producer_terminal`, and `producer_unknown`; API metrics exposed bounded source/intent/dispatch/notification counters. Public Playwright on `/strategies?strategy_id=d6a4633a-1c14-4cc9-8a53-4c8b1693485d` showed `Execution outcomes` ready with 6 rows, dashboard requests `200`, coverage for all notification types plus `no_intent`, DOM secret scan `false`, and screenshots/DOM artifacts under `output/playwright/`. Temporary smoke session was revoked to active count `0`; temp auth files were deleted. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/16-producer-integrations-notifications.md`; local gates: focused pytest/ruff/pyright, `node --check apps/web/dist/js/pages/strategies.js`, required `uv run ruff check src/trading/contexts/strategy src/trading/contexts/live_execution apps tests`, required `uv run pyright src/trading/contexts/strategy src/trading/contexts/live_execution apps tests`, required `uv run pytest -q tests/unit/contexts/strategy tests/unit/contexts/live_execution tests/unit/apps` (`421 passed, 3 warnings`), docs index and diff check. Runtime evidence includes Mac Studio smoke, production API/SQL/Redis/metrics probe, Strategy ACL adapter no-intent proof, public Playwright screenshot and DOM artifact. | Implementation commit `89e40026` pushed to `main`; CI `26850850053`, Publish App Image `26850942477`, Deploy Backend `26850942515`, Deploy Web `26850942466` and follow-up Deploy Web `26850986731` succeeded. Publish App Image had a non-fatal Docker cache reservation annotation. Evidence update follows in docs-only commit. | Stage `17` can start. It must preserve producer-neutral ingress, risk, Redis dispatch, exchange-execution ledger, reconciliation, notification outbox, and `/strategies` outcome-link semantics. Redis remains transport only; durable Postgres source/intent/order/fill/reconciliation/outbox rows are source of truth. |
| `17` | blocked | Delivered `exchange-execution` pending recovery, Stage 17 Prometheus rules, bootstrap install path, Monit/runbook alert action contract, and a production-found unsupported-exchange guard fix. Mac Studio after deploy showed `/health/ready` `ready/all_dependencies_ready`, Redis `XPENDING` `0`, DLQ length unchanged at the pre-existing Stage 12 entry, old pending messages resolved by durable observations, Prometheus rule group loaded with seven inactive alerts, `up{job="exchange-execution"}=1`, Monit `roehub_exchange_execution OK`, and `smoke_prod.sh` passed. Not accepted because no fresh full E2E Playwright/API/DB/Redis/testnet-fill/reconciliation/notification/latency/slippage journey was completed. | `docs/architecture/live_execution/live-execution-universal-order-gateway-v1-stage-reports/17-production-readiness-e2e-deploy-proof.md`; local gates include focused and broad ruff/pyright, focused runtime/unit tests, `tests/unit`, infra monitoring tests, docs index and diff check. Runtime evidence includes GitHub CI/deploy watch, Mac Studio health/run-once/Redis/SQL/Prometheus/Monit/smoke checks. Literal `tests/integration` and `tests/e2e` paths are absent in this checkout. | Implementation commit `dac40caa` and follow-up fix commit `88ef1fe0` pushed to `main`; CI `26904424782` and `26904770456` succeeded; Publish App Image `26904532691` and `26904959252` succeeded; Deploy Backend `26904532675` and `26904956680` succeeded; Deploy Web `26904532673`, `26904545441`, and `26904960071` succeeded. | Next attempt must define/approve a safe testnet fill canary that avoids the known `cancel_after_submit=true` market-fill cancel error, then capture one correlated browser/API/DB/Redis/exchange/metrics journey with latency and slippage. Mainnet remains blocked. |
