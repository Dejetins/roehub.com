---
ticket_id: ROEHUB-PENPOT-LINEAR-VNEXT-FOUNDATIONS-2026-07-20
status: draft
owner: unassigned
ticket_graph: .codex/delivery/graphs/roehub-authenticated-platform-delivery-v1.json
depends_on:
  - ROEHUB-LINEAR-REFERENCE-COMPLETION-2026-07-20
  - ROEHUB-LINEAR-FRONTEND-ARCHITECTURE-SPIKE-2026-07-20
evidence: []
---

# Create Roehub Penpot Linear-workspace vNext

## Outcome

A separately identified Penpot file defines implementable four-theme Roehub
foundations, components, shell, resizable panels, system states, and the
read-only Backtest list/detail golden slice.

## Start probe

Use `penpot-design-delivery`. Confirm Penpot availability and whether the exact
vNext file exists before broad reads. Never write to a historical Roehub or
Custometry file. Before the first mutation, fingerprint all declared context
sources and stop on file-ID, revision, fingerprint, or concurrent drift.

## Owned paths

- the separately named Roehub authenticated-platform Penpot vNext file;
- this ticket and terminal evidence;
- `docs/architecture/ui/roehub-linear-ui-migration-registry-v1.json` only to
  record canonical file identity and terminal revision.

## Scope

- Create four semantic theme modes `abyss`, `graphite`, `frost`, and `paper`.
- Build foundations, components, patterns, stable shell, resizable sidebar and
  detail pane, command palette, overlays, system states, accessibility/motion
  variants, and the representative Backtest list/detail flow.
- Preserve Roehub screen, route, capability, trading, and brand semantics.

## Non-goals

- No Web/backend code, public site, route/capability change, trading mutation,
  or rewrite of accepted historical evidence.

## Proof boundary

Inspect every target component/frame and changed region at useful zoom. Record
file ID, start/end revision, context fingerprint, inventories, four-theme
matrix, visual QA, and the explicit absence of browser/runtime proof.

## Acceptance

All design targets pass structural and visual checks without orphan/duplicate
IDs, terminal evidence exists, and the ticket becomes `accepted` only after a
stable final revision and repeated fingerprint.
