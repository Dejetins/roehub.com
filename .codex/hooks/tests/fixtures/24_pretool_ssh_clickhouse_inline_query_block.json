{
  "payload": {
    "hook_event_name": "PreToolUse",
    "tool_name": "Bash",
    "tool_input": {
      "command": "ssh macstudio 'set -a; source /Users/daniildegtyarev/.config/roehub/roehub.env; set +a; /opt/clickhouse/clickhouse client --query \"SELECT count(), max(ts_open) FROM market_data.canonical_candles_1m WHERE symbol='1000BONKUSDT' AND source='rest'\"'"
    }
  },
  "expect": {
    "returncode": 0,
    "json_path": "hookSpecificOutput.permissionDecision",
    "json_value": "deny",
    "stdout_contains": "SSH SQL payload uses inline nested quoting"
  }
}
