# Direction attempt 4 — contextual-action gate rejection

## Outcome

- Status: `gate_failed` before product-owner review.
- Final local raster:
  `.codex/delivery/evidence/roehub-ui-agent-governed-pilot/artifacts/dir-002-roehub-linear-native-backtests.png`.
- Figma identity: file `nzKVsXuCmoTbHJGckHfK3T`, archived frame `10:2` on page `3:6`.
- Figma status: `DIR-002 / Roehub Linear-native Backtests / v1 / GATE_FAILED`.
- Reuse: prohibited.

## Passed observations

- Replaced the rejected full-width dashboard topology with a compact worklist, selected-job
  detail, and properties/freshness rail.
- Kept all required Backtests data recoverable through progressive disclosure.
- Removed unsupported shell actions and corrected the complete `created_at` value.
- Passed hierarchy, density, visual-noise, typography, colour, clipping, and Figma read-back checks.

## Failed gate

The selected row retained an ellipsis action. Its conventional meaning is an unspecified menu,
while the pilot contract permits exactly `open_details` and no additional row actions. The two
bounded automatic repair attempts had already been used, so the artifact was archived instead of
being shown as a gated candidate.

## Correction boundary

`DIR-003` was generated from the text doctrine and the single external craft reference only. It
did not receive `DIR-002` as a visual input and uses one explicit arrow/chevron for `open_details`.
