# Linear-black Workbench v6 owner feedback

- Date: `2026-07-31`.
- Checkpoint: `direction_selection`.
- Result: `preferred_direction_refinement_required`.

The product owner retained the overall concept and supplied ten coordinate-specific corrections.
The requested product changes remove `Symbol`, `Run strategy`, `Notebooks`, and `Market`; move
Filters and New backtest into Jobs; add `Queued`; replace the duplicative Risk summary; compress the
KPI and bottom status blocks; and correct the persistent vertical asymmetry of text and controls.

The current contract is
`docs/architecture/ui/roehub-linear-black-backtests-workbench-doctrine-v6.md`. The v5 raster remains
the edit target and visual-system evidence, not an accepted artifact.
