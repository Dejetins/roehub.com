# Legacy agent artifacts

This directory has no active repository role configuration, prompt template, or
agent state contract. Runtime routing comes from the platform skill list and
the repository root instructions.

`legacy/` preserves superseded templates only as historical evidence. Do not
use a file there to create, format, or authorize new work. A new procedure or
staged workflow must first be selected by the global delivery contract.
