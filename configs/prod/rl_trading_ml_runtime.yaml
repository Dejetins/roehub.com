---
profile: prod
artifact_root: /opt/roehub/state/rl_trading
device_policy: mps_preferred_cpu_fallback
trainer:
  enabled: false
  max_concurrent_jobs: 1
  torch_num_threads: 4
  torch_num_interop_threads: 1
  max_rss_mb: 32768
  nice: 10
retraining:
  enabled: false
  manual_trigger:
    enabled: false
    host_local_cli_only: true
  scheduled_trigger:
    enabled: false
    schedule_id: null
  drift_trigger:
    creates_candidate_task: true
    auto_promote: false
  allowed_modes:
    - full_retrain
    - fine_tune
  max_concurrent_candidate_jobs: 1
promotion:
  auto_promote: false
  require_operator_approval: true
  require_admin_approval: true
  threshold_profile: stage10a_promotion_threshold_profile_v1
rollback:
  host_local_command_enabled: true
  delete_artifacts_on_rollback: false
inference:
  enabled: false
  max_concurrent_processes: 1
  torch_num_threads: 1
  torch_num_interop_threads: 1
  max_rss_mb: 4096
backtest_jobs:
  max_concurrent_when_training: 1
  max_concurrent_when_inference_only: 2
disk:
  min_free_gb: 100
  block_training_below_free_gb: 150
  cleanup_may_delete_accepted_artifacts: false
runtime_artifacts:
  commit_to_git: false
  allowed_root: /opt/roehub/state/rl_trading
