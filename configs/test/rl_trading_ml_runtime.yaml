---
profile: test
artifact_root: /opt/roehub/state/rl_trading
device_policy: cpu_only_for_deterministic_tests
trainer:
  enabled: false
  max_concurrent_jobs: 1
  torch_num_threads: 1
  torch_num_interop_threads: 1
  max_rss_mb: 2048
  nice: 10
inference:
  enabled: false
  max_concurrent_processes: 1
  torch_num_threads: 1
  torch_num_interop_threads: 1
  max_rss_mb: 1024
backtest_jobs:
  max_concurrent_when_training: 1
  max_concurrent_when_inference_only: 1
disk:
  min_free_gb: 20
  block_training_below_free_gb: 30
  cleanup_may_delete_accepted_artifacts: false
runtime_artifacts:
  commit_to_git: false
  allowed_root: /opt/roehub/state/rl_trading
