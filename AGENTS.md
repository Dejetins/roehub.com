# Repository Agent Instructions

This root file exists only for standard `AGENTS.md` discovery.

The normative repository engineering contract lives in `.codex/AGENTS.md`.

For work in this repository:
- read and follow `.codex/AGENTS.md` after this file;
- treat `.codex/PLANS.md` and generated prompt packs as historical evidence
  unless a current ticket explicitly selects them;
- use the architecture index only when the task crosses a documented boundary;
- if `.codex/AGENTS.md` cannot be read, fall back to the platform/global instructions and state that limitation.
