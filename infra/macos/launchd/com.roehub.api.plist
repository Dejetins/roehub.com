<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.roehub.api</string>

  <key>RunAtLoad</key>
  <true/>
  <key>KeepAlive</key>
  <true/>

  <key>LimitLoadToSessionType</key>
  <string>Aqua</string>

  <key>WorkingDirectory</key>
  <string>/opt/roehub/app</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/zsh</string>
    <string>-lc</string>
    <string>set -a; source /Users/daniildegtyarev/.config/roehub/roehub.env; set +a; export ROEHUB_ENV=prod; export ROEHUB_ENV_FILE=/Users/daniildegtyarev/.config/roehub/roehub.env; export PATH=/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$HOME/.local/bin; export PYTHONUNBUFFERED=1; export CH_HOST=127.0.0.1; export CH_PORT=8123; export CH_DATABASE=${CH_DATABASE:-market_data}; export CH_USER=${CH_USER:-${CLICKHOUSE_USER:-roe}}; export CH_PASSWORD=${CH_PASSWORD:-${CLICKHOUSE_PASSWORD:-}}; export IDENTITY_PG_DSN=${IDENTITY_PG_DSN:-"host=127.0.0.1 port=5432 dbname=${POSTGRES_DB} user=${POSTGRES_USER} password=${POSTGRES_PASSWORD}"}; export STRATEGY_PG_DSN=${STRATEGY_PG_DSN:-"host=127.0.0.1 port=5432 dbname=${POSTGRES_DB} user=${POSTGRES_USER} password=${POSTGRES_PASSWORD}"}; export POSTGRES_DSN=${POSTGRES_DSN:-"host=127.0.0.1 port=5432 dbname=${POSTGRES_DB} user=${POSTGRES_USER} password=${POSTGRES_PASSWORD}"}; export ROEHUB_EXCHANGE_CONTROL_INTERNAL_BASE_URL=${ROEHUB_EXCHANGE_CONTROL_INTERNAL_BASE_URL:-http://127.0.0.1:9205}; export KEYCLOAK_BASE_URL=${KEYCLOAK_BASE_URL:-}; export KEYCLOAK_REALM=${KEYCLOAK_REALM:-}; export KEYCLOAK_CLIENT_ID=${KEYCLOAK_CLIENT_ID:-}; export KEYCLOAK_CLIENT_SECRET=${KEYCLOAK_CLIENT_SECRET:-}; export KEYCLOAK_REDIRECT_URI=${KEYCLOAK_REDIRECT_URI:-}; export KEYCLOAK_LOGOUT_REDIRECT_URI=${KEYCLOAK_LOGOUT_REDIRECT_URI:-}; export KEYCLOAK_AUTH_URL=${KEYCLOAK_AUTH_URL:-}; export KEYCLOAK_TOKEN_URL=${KEYCLOAK_TOKEN_URL:-}; export KEYCLOAK_END_SESSION_URL=${KEYCLOAK_END_SESSION_URL:-}; export KEYCLOAK_INTROSPECTION_URL=${KEYCLOAK_INTROSPECTION_URL:-}; export IDENTITY_SESSION_COOKIE_NAME=${IDENTITY_SESSION_COOKIE_NAME:-roehub_session_id}; export IDENTITY_SESSION_IDLE_TTL_SECONDS=${IDENTITY_SESSION_IDLE_TTL_SECONDS:-1800}; export IDENTITY_SESSION_ABSOLUTE_TTL_SECONDS=${IDENTITY_SESSION_ABSOLUTE_TTL_SECONDS:-43200}; cd /opt/roehub/app; exec /opt/roehub/app/.venv/bin/python -m apps.api.main.main --host 127.0.0.1 --port 8000</string>
  </array>

  <key>StandardOutPath</key>
  <string>/Users/daniildegtyarev/Library/Logs/roehub/api.out.log</string>
  <key>StandardErrorPath</key>
  <string>/Users/daniildegtyarev/Library/Logs/roehub/api.err.log</string>
</dict>
</plist>
