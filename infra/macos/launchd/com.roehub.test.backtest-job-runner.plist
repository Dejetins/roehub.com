<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.roehub.test.backtest-job-runner</string>

  <key>RunAtLoad</key>
  <true/>
  <key>KeepAlive</key>
  <true/>

  <key>LimitLoadToSessionType</key>
  <string>Aqua</string>

  <key>WorkingDirectory</key>
  <string>/opt/roehub/app</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/zsh</string>
    <string>-lc</string>
    <string>set -a; source /Users/daniildegtyarev/.config/roehub/roehub.test.env; set +a; export PATH=/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$HOME/.local/bin; export PYTHONUNBUFFERED=1; export ROEHUB_ENV=test; export ROEHUB_BACKTEST_RUNNER_ENABLED=true; export ROEHUB_BACKTEST_RUNNER_CONCURRENCY=1; export ROEHUB_BACKTEST_RUNNER_POLL_INTERVAL_SECONDS=2; export ROEHUB_BACKTEST_RUNNER_EMPTY_BACKOFF_SECONDS=5; export ROEHUB_BACKTEST_RUNNER_LEASE_SECONDS=120; export ROEHUB_BACKTEST_RUNNER_HEARTBEAT_INTERVAL_SECONDS=30; export ROEHUB_BACKTEST_RUNNER_MAX_JOBS_PER_PROCESS=10; export ROEHUB_BACKTEST_RUNNER_METRICS_PORT=19204; export ROEHUB_BACKTEST_RUNNER_LAZY_DETAIL_ANTI_STARVATION_LIMIT=5; export CH_HOST=127.0.0.1; export CH_PORT=18124; export CH_DATABASE=${CH_DATABASE:-market_data}; export CH_USER=${CH_USER:-${CLICKHOUSE_USER:-roe}}; export CH_PASSWORD=${CH_PASSWORD:-${CLICKHOUSE_PASSWORD:-}}; cd /opt/roehub/app; exec /opt/roehub/app/.venv/bin/python -m apps.worker.backtest_job_runner.main.main --metrics-port 19204</string>
  </array>

  <key>StandardOutPath</key>
  <string>/Users/daniildegtyarev/Library/Logs/roehub/test-backtest-job-runner.out.log</string>
  <key>StandardErrorPath</key>
  <string>/Users/daniildegtyarev/Library/Logs/roehub/test-backtest-job-runner.err.log</string>
</dict>
</plist>
